spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/garinogeorgiana/ProyectoFinalMicroservicios
          default-label: main

server:
  port: 8888
